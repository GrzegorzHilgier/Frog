<UserControl x:Class="Frog.Views.GameControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ViewModels="clr-namespace:Frog.ViewModels"
             xmlns:local="clr-namespace:Frog.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400"
             Focusable="True"
             d:DataContext="{d:DesignInstance ViewModels:Game}"
             Loaded="UserControl_Loaded">

    
    <UserControl.InputBindings>
        <KeyBinding Key="W" Command="{Binding MoveUpCommand}"/>
        <KeyBinding Key="S" Command="{Binding MoveDownCommand}"/>
        <KeyBinding Key="A" Command="{Binding MoveLeftCommand}"/>
        <KeyBinding Key="D" Command="{Binding MoveRightCommand}"/>
    </UserControl.InputBindings>

        <Canvas>
            <ItemsControl ItemsSource="{Binding ItemsToDraw}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Image Height="{Binding Height}" Width="{Binding Width}" Source="{StaticResource FrogImg}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemContainerStyle>
                <Style>
                    <Setter Property="Canvas.Top" Value="{Binding Xcoord}"/>
                    <Setter Property="Canvas.Left" Value="{Binding Ycoord}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        <Image Name="Player" Height="30" Width="30" Source="{StaticResource FrogImg}" Canvas.Left="{Binding X}" Canvas.Top="{Binding Y}" ></Image>
    </Canvas>

       


</UserControl>
