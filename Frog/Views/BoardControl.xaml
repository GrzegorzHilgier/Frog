<UserControl x:Class="Frog.Views.BoardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ViewModels="clr-namespace:Frog.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="60"
             d:DesignWidth="500"
             d:DataContext="{d:DesignInstance ViewModels:Game}">
    
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <TextBlock >Lives</TextBlock>
            <TextBlock Grid.Column="1">Score</TextBlock>
            <TextBlock Grid.Row="1" Text="{Binding Lives}"></TextBlock>
            <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Score}"></TextBlock>
        </Grid>
    
</UserControl>
